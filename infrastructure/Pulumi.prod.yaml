# Prod stack config — env-specific knobs only.
# Project identity (merchant name, domain, emails) lives in /project.config.json.
config:
  aws:region: us-east-1
  bootstrap:environment: prod
  bootstrap:cloudfrontPriceClass: PriceClass_All
  bootstrap:corsAllowedOrigins: []
